new constraint type: range(attName minInclusive maxInclusive)

na for min means no min
na for max mean no max

only can be used at level 2 (user preferences)

for now only 2 attNames: msrp and mpg

action item: check with Ken on msrp: model only versus sum



interface VarInfo{
 	String getAttribute(String varName,String attName);
}

Csp createCsp(String constraints); changes to:
Csp createCsp(String constraints,VarInfo callback)

